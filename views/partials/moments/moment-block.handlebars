<div class="mr-6 my-4 md:my-8">
    <!-------------------------------------------------Moments Input-->
    <div class="max-w-6xl">
        
        <!------------------------------------------------------Moment-->

        <div class="col-span-2 mt-6">
            <div class="max-w-4xl p-5 md:px-10 md:py-6 bg-white rounded-lg shadow-md">
                <div class="flex justify-between items-center">

                    <span class="font-light text-sm md:text-lg text-gray-600">{{dataValues.createdAt}}</span>
                    <a href="#" class="px-2 py-1 bg-gray-600 text-gray-100 font-bold rounded hover:bg-gray-500"
                        id="moment-time">#
                        Moment</a>
                </div>
                <div class="mt-2"><a href="#" class="text-sm md:text-2xl text-gray-700 font-bold hover:underline"
                        id="moment-title">
                        {{dataValues.title}}</a>
                    <p class="mt-2 text-sm md:text-lg text-gray-600">
                        {{dataValues.body}}
                    </p>
                </div>
                <div class="flex justify-between items-center mt-4">
                    <!-- <a href="#" class="text-blue-500 hover:underline">Read more</a> -->
                    <div>
                        <a href="/user/{{dataValues.UserId}}" class="flex items-center"> View user profile 
                        {{!-- <img src="{{dataValues.imageurl}}" alt="avatar" class="mx-4 w-10 h-10 object-cover rounded-full hidden sm:block"> --}}
                        </a>
                    </div>

                    <div>
                        <!-- Delete Button -->
                        <a href="#"
                            class="px-2 py-1 bg-gray-400 text-sm md:text-lg text-gray-100 font-bold rounded hover:bg-gray-600">Delete</a>
                        </a>
                    </div>
                </div>
            </div>
        </div>       
    </div>
</div>